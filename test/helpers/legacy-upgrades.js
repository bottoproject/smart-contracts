const LEGACY_UPGRADE_OPTIONS = Object.freeze({
  unsafeAllow: [
    "missing-initializer",
    "missing-initializer-call",
    "incorrect-initializer-order",
  ],
});

module.exports = { LEGACY_UPGRADE_OPTIONS };
